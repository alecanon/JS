// Objeto Math

let resultado;

resultado = Math.PI;        // MUESTRA NÚMERO PI
resultado = Math.round(2.5);//
resultado = Math.ceil(2.1); // Propiedad o metodo para redondear hacía arriba
resultado = Math.floor(2.9);// Propiedad o metodo para redondear hacía abajo
resultado = Math.sqrt(144); // Propiedad o metodo para sacar raiz cuadrada
resultado = Math.abs(-200); // Propiedad o metodo para convertir a positivo número negativo
resultado = Math.min(3, 5, 1, 8 , 2, 10 ); // La siguiente línea de código calcula el valor mínimo entre una lista de números.
resultado = Math.max(3, 5, 1, 8 , 2, 10 ); // La siguiente línea de código calcula el valor máximo entre una lista de números.
resultado = Math.random();  // La siguiente línea de código genera un número aleatorio entre 0 y 1.
resultado = Math.floor( Math.random() * 30 );

console.log(resultado);
